// import { useAuth } from "../context/Context";
import Landing from "../components/Landing";

const LandingPage = () => {
  // const { login } = useAuth();

  return <Landing />
};

export default LandingPage;
